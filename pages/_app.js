/* app global dependencies */
import "../styles/globals.css";

/* my app instance - whole application */
function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
